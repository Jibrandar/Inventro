<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Activate License - Inventra</title>
  <style>
    body {
      padding: 0;
      margin: 0;
      color: black;
      background-color: #f6f7fb;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      min-height: 100vh;
    }

    .activation-card {
      width: 400px;
      background-color: white;
      padding: 30px 25px;
      border-radius: 12px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.13);
      text-align: center;
      transition: 0.3s ease-in-out;
      margin-bottom: 40px;
    }

    .activation-card:hover {
      transform: scale(1.01);
    }

    .activation-card h2 {
      margin-bottom: 20px;
      font-size: 1.8rem;
      color: #333;
    }

    label {
      display: block;
      text-align: left;
      margin-bottom: 8px;
      font-weight: 600;
      color: #555;
    }

    input[type="text"] {
      width: 90%;
      padding: 12px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 12px;
      font-size: 1rem;
    }

    input[type="submit"] {
      width: 100%;
      padding: 12px;
      background-color: #1abc9c;
      color: white;
      font-weight: 600;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      transition: all 0.3s ease-in-out;
    }
   

    input[type="submit"]:hover {
      background-color: #16a085;
      transform: scale(1.03);
    }
    a.btn {
  display: block;
  
  padding: 12px;
  background-color: #1abc9c;
  color: white;
  font-weight: 600;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  text-align: center;
  text-decoration: none;
  margin-top: 10px;
  transition: all 0.3s ease-in-out;
}

a.btn:hover {
  background-color: #16a085;
  transform: scale(1.03);
}
  </style>
</head>
<body>

  <div class="activation-card">
    <h2>Activate Inventra</h2>
    <form action="/activate" method="post">
      <label for="license_key">License Key</label>
      <input type="text" name="license_key" id="license_key" placeholder="Enter License Key" required />
      <input type="submit" value="Validate License" />
      <a href="http://localhost:8080/renew" class="btn">Renew  License</a>
    </form>
    
  </div>

    <%- include('partials/footer') %>


</body>
</html>
